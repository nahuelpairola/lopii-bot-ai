{
  "schemaVersion": 2,
  "generatedAt": "2026-09-12T22:11:30.746907Z",
  "title": "Design System: Lopii Mini App",
  "extensions": {
    "colorMeta": {
      "theme-accent": {
        "role": "primary",
        "displayName": "Theme Accent",
        "canonical": "var(--tg-theme-accent-text-color, var(--tg-theme-link-color, #0172ad))",
        "tonalRamp": [
          "#012c43",
          "#014363",
          "#015887",
          "#0172ad",
          "#2a90c9",
          "#5aaedb",
          "#96cdea",
          "#d4eaf7"
        ],
        "note": "Resolved by the user's Telegram client; the hex is only the outside-Telegram fallback."
      },
      "theme-accent-dark": {
        "role": "primary",
        "displayName": "Theme Accent (dark fallback)",
        "canonical": "var(--tg-theme-accent-text-color, var(--tg-theme-link-color, #01aaff))",
        "tonalRamp": [
          "#002034",
          "#003a5e",
          "#00558b",
          "#0177bd",
          "#01aaff",
          "#4dc3ff",
          "#93daff",
          "#d6f0ff"
        ]
      },
      "theme-button": {
        "role": "primary",
        "displayName": "Theme Button",
        "canonical": "var(--tg-theme-button-color, #0172ad)",
        "tonalRamp": [
          "#012c43",
          "#014363",
          "#015887",
          "#0172ad",
          "#2a90c9",
          "#5aaedb",
          "#96cdea",
          "#d4eaf7"
        ],
        "note": "Fill of a selected chip. Selection is a fill, never a hue shift."
      },
      "theme-button-text": {
        "role": "primary",
        "displayName": "Theme Button Text",
        "canonical": "var(--tg-theme-button-text-color, #ffffff)",
        "tonalRamp": [
          "#26292e",
          "#3d4147",
          "#565b63",
          "#7b8087",
          "#a3a8ae",
          "#c8ccd1",
          "#e6e8eb",
          "#ffffff"
        ]
      },
      "statement-ink": {
        "role": "neutral",
        "displayName": "Statement Ink",
        "canonical": "var(--tg-theme-text-color, #373c44)",
        "tonalRamp": [
          "#0f1114",
          "#1d2025",
          "#2d3138",
          "#373c44",
          "#5b626e",
          "#89919c",
          "#b8bec8",
          "#e7eaef"
        ],
        "note": "Body text and every figure not carrying status."
      },
      "statement-ink-dark": {
        "role": "neutral",
        "displayName": "Statement Ink (dark fallback)",
        "canonical": "var(--tg-theme-text-color, #c2c7d0)",
        "tonalRamp": [
          "#14171c",
          "#22262d",
          "#343941",
          "#4a505a",
          "#6d7481",
          "#949ba7",
          "#c2c7d0",
          "#eef0f3"
        ]
      },
      "margin-grey": {
        "role": "neutral",
        "displayName": "Margin Grey",
        "canonical": "var(--tg-theme-hint-color, #646b79)",
        "tonalRamp": [
          "#16181c",
          "#25282e",
          "#383c44",
          "#4c515b",
          "#646b79",
          "#8b919c",
          "#b4b9c1",
          "#e2e4e8"
        ],
        "note": "Where most of the interface lives: captions, meta lines, disabled arrows."
      },
      "margin-grey-dark": {
        "role": "neutral",
        "displayName": "Margin Grey (dark fallback)",
        "canonical": "var(--tg-theme-hint-color, #7b8495)",
        "tonalRamp": [
          "#181b20",
          "#272b33",
          "#383d47",
          "#4b515d",
          "#616875",
          "#7b8495",
          "#a8aeba",
          "#dcdfe5"
        ]
      },
      "hairline": {
        "role": "neutral",
        "displayName": "Hairline",
        "canonical": "var(--tg-theme-section-separator-color, var(--tg-theme-hint-color, rgb(231, 234, 239.5)))",
        "tonalRamp": [
          "#1c1f25",
          "#2c3038",
          "#3e434d",
          "#565c68",
          "#767c88",
          "#a0a6b0",
          "#c9ced6",
          "#e7eaef"
        ],
        "note": "Every 1px rule in the system. Falls back through the hint color because section-separator-color is Bot API 7.6+."
      },
      "hairline-dark": {
        "role": "neutral",
        "displayName": "Hairline (dark fallback)",
        "canonical": "var(--tg-theme-section-separator-color, var(--tg-theme-hint-color, #202632))",
        "tonalRamp": [
          "#0c0e13",
          "#14181f",
          "#202632",
          "#2e3542",
          "#414a5b",
          "#5c6678",
          "#868d9a",
          "#b6bcc7"
        ]
      },
      "paper": {
        "role": "neutral",
        "displayName": "Paper",
        "canonical": "var(--tg-theme-secondary-bg-color, #ffffff)",
        "tonalRamp": [
          "#2b2e33",
          "#43474d",
          "#5d6168",
          "#7d8188",
          "#a5a9af",
          "#cbced2",
          "#eceef0",
          "#ffffff"
        ],
        "note": "The page and the tab bar. Opaque: content scrolls beneath the bar."
      },
      "paper-dark": {
        "role": "neutral",
        "displayName": "Paper (dark fallback)",
        "canonical": "var(--tg-theme-secondary-bg-color, rgb(19, 22.5, 30.5))",
        "tonalRamp": [
          "#0a0c10",
          "#13171f",
          "#1d222c",
          "#29303c",
          "#3a4250",
          "#525b6b",
          "#7b8390",
          "#adb3bd"
        ]
      },
      "card-paper": {
        "role": "neutral",
        "displayName": "Card Paper",
        "canonical": "var(--tg-theme-section-bg-color, rgb(251, 251.5, 252.25))",
        "tonalRamp": [
          "#2c2e30",
          "#454749",
          "#5f6164",
          "#808285",
          "#a7a9ab",
          "#cccdcf",
          "#ececed",
          "#fbfbfc"
        ],
        "note": "Cards rest on top of the page: Telegram's own page/section split."
      },
      "card-paper-dark": {
        "role": "neutral",
        "displayName": "Card Paper (dark fallback)",
        "canonical": "var(--tg-theme-section-bg-color, rgb(26, 30.5, 40.25))",
        "tonalRamp": [
          "#0d0f14",
          "#161a22",
          "#1a1e28",
          "#272d3a",
          "#39404f",
          "#525a6b",
          "#7c8492",
          "#aeb4be"
        ]
      },
      "positive-green": {
        "role": "secondary",
        "displayName": "Positive Green",
        "canonical": "color-mix(in srgb, #0ca30c 70%, var(--pico-color))",
        "tonalRamp": [
          "#032f03",
          "#054b05",
          "#076807",
          "#0a850a",
          "#0ca30c",
          "#3cc23c",
          "#7ad97a",
          "#c2f0c2"
        ],
        "note": "Derived, not fixed. Mixed toward the theme's text color: 4.82:1 light, 6.34:1 dark. Only ever the Neto."
      },
      "negative-red": {
        "role": "secondary",
        "displayName": "Negative Red",
        "canonical": "color-mix(in srgb, var(--tg-theme-destructive-text-color, #d03b3b) 70%, var(--pico-color))",
        "tonalRamp": [
          "#3a1010",
          "#5c1a1a",
          "#7e2323",
          "#a52d2d",
          "#d03b3b",
          "#e07070",
          "#eda3a3",
          "#f8d5d5"
        ],
        "note": "Prefers Telegram's destructive color, then mixes. 6.50:1 light, 4.83:1 dark. Only ever the Neto."
      },
      "ledger-green": {
        "role": "secondary",
        "displayName": "Ledger Green",
        "canonical": "#1baf7a",
        "tonalRamp": [
          "#052419",
          "#093c2a",
          "#0e553b",
          "#14764f",
          "#1baf7a",
          "#3fcd9a",
          "#7de0bd",
          "#c4f1e1"
        ],
        "note": "The income series. Fixed because Telegram exposes a destructive color and no positive one."
      },
      "slot-1": {
        "role": "tertiary",
        "displayName": "Account Slot 1",
        "canonical": "#2a78d6",
        "tonalRamp": [
          "#0a1c33",
          "#102d52",
          "#164073",
          "#1e5aa1",
          "#2a78d6",
          "#5f9be3",
          "#9bc1ee",
          "#d7e6f9"
        ],
        "note": "Categorical identity: the card and the trend line must match."
      },
      "slot-2": {
        "role": "tertiary",
        "displayName": "Account Slot 2",
        "canonical": "#1baf7a",
        "tonalRamp": [
          "#052419",
          "#093c2a",
          "#0e553b",
          "#14764f",
          "#1baf7a",
          "#3fcd9a",
          "#7de0bd",
          "#c4f1e1"
        ]
      },
      "slot-3": {
        "role": "tertiary",
        "displayName": "Account Slot 3",
        "canonical": "#eda100",
        "tonalRamp": [
          "#3a2800",
          "#5c3f00",
          "#7e5600",
          "#b07800",
          "#eda100",
          "#f5bc42",
          "#fad489",
          "#fdeecb"
        ]
      },
      "slot-4": {
        "role": "tertiary",
        "displayName": "Account Slot 4",
        "canonical": "#008300",
        "tonalRamp": [
          "#001f00",
          "#003400",
          "#004a00",
          "#006600",
          "#008300",
          "#2ba52b",
          "#6ec96e",
          "#bfe9bf"
        ]
      },
      "slot-5": {
        "role": "tertiary",
        "displayName": "Account Slot 5",
        "canonical": "#4a3aa7",
        "tonalRamp": [
          "#121029",
          "#1e1a45",
          "#2b2463",
          "#3a2f85",
          "#4a3aa7",
          "#7263c4",
          "#a49ada",
          "#d8d3f0"
        ]
      },
      "slot-6": {
        "role": "tertiary",
        "displayName": "Account Slot 6",
        "canonical": "#e34948",
        "tonalRamp": [
          "#3d1010",
          "#601a1a",
          "#842424",
          "#b13232",
          "#e34948",
          "#ec7a79",
          "#f4a9a9",
          "#fbdada"
        ]
      },
      "slot-7": {
        "role": "tertiary",
        "displayName": "Account Slot 7",
        "canonical": "#e87ba4",
        "tonalRamp": [
          "#3d1524",
          "#61213a",
          "#852e51",
          "#b03e6d",
          "#e87ba4",
          "#f0a0bf",
          "#f6c3d6",
          "#fbe4ec"
        ]
      },
      "slot-8": {
        "role": "tertiary",
        "displayName": "Account Slot 8",
        "canonical": "#eb6834",
        "tonalRamp": [
          "#3d180a",
          "#611f0f",
          "#852b15",
          "#b03a1c",
          "#eb6834",
          "#f28f66",
          "#f7b79a",
          "#fcdfd0"
        ]
      },
      "heat-mild": {
        "role": "neutral",
        "displayName": "Heat Mild",
        "canonical": "color-mix(in srgb, var(--pico-color) 8%, transparent)",
        "tonalRamp": null,
        "note": "Evolucion cell above its row average. Mixed from the INK, not the accent: the accent is arbitrary per user, so an accent tint could land on the text's own luminance with nothing to warn you. Mixing from the ink makes the tint a fixed fraction of the ink-to-paper gap, so it always moves the right way."
      },
      "heat-high": {
        "role": "neutral",
        "displayName": "Heat High",
        "canonical": "color-mix(in srgb, var(--pico-color) 18%, transparent)",
        "tonalRamp": null,
        "note": "The second and last heat step. ~7.5:1 light / ~7.2:1 dark against the ink, measured on pico's fallbacks; for any other theme the guarantee is structural, not a number."
      },
      "focus-ring": {
        "role": "primary",
        "displayName": "Focus Ring",
        "canonical": "color-mix(in srgb, var(--tg-theme-accent-text-color, var(--tg-theme-link-color, #0172ad)) 50%, transparent)",
        "tonalRamp": null,
        "note": "--pico-primary-focus. Was the single token that escaped the theme map, leaving the focus ring pico's fixed azure on every theme."
      },
      "selection": {
        "role": "primary",
        "displayName": "Selection",
        "canonical": "color-mix(in srgb, var(--pico-primary) 30%, transparent)",
        "tonalRamp": null,
        "note": "::selection. A browser surface nobody draws; left alone it ships Chrome blue over a themed page."
      }
    },
    "typographyMeta": {
      "kpi": {
        "displayName": "KPI",
        "purpose": "Money inside a card. 1.75rem is a ceiling: $1.234.567,00 fits a 360px card at this size and not at 2rem."
      },
      "page-total": {
        "displayName": "Page Total",
        "purpose": "The one figure that answers the screen: Categorias' and its drill's Total, Cuentas' Saldo hoy. A step below the KPI because it shares a line with its own label and would crush it; the KPI stands alone in its card and keeps the top step."
      },
      "heading": {
        "displayName": "Heading",
        "purpose": "The one heading level, #content h1. Opens AccountLeaf, SubcategoryDrill, SubcategoryLeaf and Evolucion; Resumen has none, deliberately. It sits BELOW both money steps on purpose: at 1.75rem it tied the KPI figure and the page total, so the largest type on screen was simultaneously a label and a number and neither dominated."
      },
      "body": {
        "displayName": "Body",
        "purpose": "Movement titles, prose, empty-state messages."
      },
      "table": {
        "displayName": "Table",
        "purpose": "The Evolucion grid and the Categorias table. At body size Evolucion's six months plus the category column measure 372px and overflow a 360px phone; Categorias compacts for the same reason (Pico's own td/th padding)."
      },
      "label": {
        "displayName": "Label",
        "purpose": "KPI captions, movement meta lines, chip text, chart legends. Always Margin Grey."
      },
      "micro": {
        "displayName": "Micro",
        "purpose": "The Variacion note and the Evolucion legend. The floor - nothing goes smaller."
      },
      "cursor": {
        "displayName": "Cursor",
        "purpose": "The period header's chevrons only. Glyphs, not text: the size is what makes one character readable inside its own 44x44 tap target."
      }
    },
    "shadows": [
      {
        "name": "sticky-column-edge",
        "value": "inset -1px 0 var(--pico-table-border-color)",
        "purpose": "Structural, not atmospheric: stands in for the border a collapsed table takes away from a sticky scrolling cell. The only shadow in the system."
      }
    ],
    "motion": [
      {
        "name": "content-loading",
        "value": "opacity 0.1s",
        "purpose": "#content drops to 0.55 opacity while an htmx request is in flight. The entire loading vocabulary."
      },
      {
        "name": "reduced-motion",
        "value": "@media (prefers-reduced-motion: reduce)",
        "purpose": "Chart animation is disabled outright rather than shortened."
      }
    ],
    "breakpoints": [
      {
        "name": "phone",
        "value": "360px",
        "purpose": "The design target. Not a breakpoint the CSS branches on - the width every measurement was checked against."
      },
      {
        "name": "pico-grid-collapse",
        "value": "768px",
        "purpose": "Pico's .grid becomes one column below this. The app is never wider, so cards are always full-width."
      }
    ]
  },
  "components": [
    {
      "name": "KPI Card",
      "kind": "card",
      "refersTo": "kpi-card",
      "description": "A caption over a large tabular figure. The Neto variant is the only element in the app that carries a status color.",
      "html": "<article class=\"ds-kpi\"><p class=\"ds-kpi-label\">Neto</p><p class=\"ds-kpi-figure ds-neto-good\">$124.500,00</p></article>",
      "css": ".ds-kpi { background: var(--pico-card-background-color, #fff); border-radius: 0.25rem; padding: 0.75rem; } .ds-kpi-label { font-size: 0.8rem; color: var(--pico-muted-color, #646b79); margin: 0 0 0.15rem; } .ds-kpi-figure { font-size: 1.75rem; font-weight: 600; line-height: 1.2; margin: 0; font-variant-numeric: tabular-nums; color: var(--pico-color, #373c44); } .ds-neto-good { color: color-mix(in srgb, #0ca30c 70%, var(--pico-color, #373c44)); }"
    },
    {
      "name": "Filter Chip",
      "kind": "chip",
      "refersTo": "chip",
      "description": "Period and currency selection, a fixed 32px pill (Telegram's own segmented-control height) rather than a 44px box. Selected is a fill, not a hue shift - Pico's one-step shift measures 1.32:1 in dark. The 44px tap target is a separate absolutely-positioned overlay, not the pill's own box.",
      "html": "<div class=\"ds-chips\"><a class=\"ds-chip\" href=\"#\">Mes</a><a class=\"ds-chip\" href=\"#\" aria-current=\"page\">6m</a></div>",
      "css": ".ds-chips { display: flex; align-items: center; gap: 0.25rem; } .ds-chip { position: relative; height: 32px; line-height: 1; display: inline-flex; align-items: center; justify-content: center; padding: 0 0.6rem; font-size: 0.8rem; border-radius: 1rem; border: 1px solid var(--pico-muted-border-color, #e7eaef); color: var(--pico-muted-color, #646b79); text-decoration: none; background: transparent; } .ds-chip::after { content: ''; position: absolute; inset: -6px 0; } .ds-chip[aria-current] { background-color: var(--tg-theme-button-color, #0172ad); border-color: var(--tg-theme-button-color, #0172ad); color: var(--tg-theme-button-text-color, #fff); } .ds-chip:hover { border-color: var(--pico-primary, #0172ad); } .ds-chip:focus-visible { outline: 2px solid var(--pico-primary, #0172ad); outline-offset: 2px; }"
    },
    {
      "name": "Tab Bar",
      "kind": "nav",
      "refersTo": "tab-link",
      "description": "Fixed bottom navigation, four tabs (Resumen, Categorias, Cuentas, Evolucion). The active tab carries a 2px bar as well as color, because muted grey against the accent is only 1.47:1.",
      "html": "<nav class=\"ds-tabbar\"><a class=\"ds-tab\" href=\"#\">Resumen</a><a class=\"ds-tab\" href=\"#\" aria-current=\"page\">Cuentas</a><a class=\"ds-tab\" href=\"#\">Evolucion</a></nav>",
      "css": ".ds-tabbar { display: flex; justify-content: space-around; border-top: 1px solid var(--pico-muted-border-color, #e7eaef); background: var(--pico-background-color, #fff); padding: 0.5rem 0; } .ds-tab { min-width: 44px; min-height: 44px; display: flex; align-items: center; justify-content: center; text-decoration: none; color: var(--pico-muted-color, #646b79); position: relative; font-size: 0.9rem; } .ds-tab[aria-current] { color: var(--pico-primary, #0172ad); font-weight: 600; text-decoration: none; } .ds-tab[aria-current]::before { content: \"\"; position: absolute; top: -0.5rem; left: 0; right: 0; height: 2px; background: var(--pico-primary, #0172ad); }"
    },
    {
      "name": "Movement Row",
      "kind": "custom",
      "refersTo": "movement-row",
      "description": "Grouped by day: each section.mov-group is headed by a GroupTitle carrying the date, and its rows no longer repeat it. Three zones per row, one line each, the amount immovable. Rows rather than a table because four fields in a narrow webview would force horizontal scrolling. Past the first 50 rows, an invisible htmx sentinel (id=\"mov-more\", hx-trigger=\"revealed\") fetches and appends the next page in place, replacing itself with the next batch plus the next sentinel (or nothing, once a page comes back short) - pagination is a loading mechanism, not a new visual affordance, and carries no styling of its own.",
      "html": "<section class=\"ds-mov-group\"><p class=\"ds-group-title\">12 ago</p><ul class=\"ds-mov-list\"><li class=\"ds-mov-row\"><span class=\"ds-mov-icon\" aria-hidden=\"true\">🍝</span><span class=\"ds-mov-text\"><span class=\"ds-mov-title\">Supermercado Coto</span><span class=\"ds-mov-meta\">Comida › Super</span></span><span class=\"ds-mov-amount\">$24.500,00</span></li></ul></section>",
      "css": ".ds-group-title { font-size: 0.8rem; color: var(--pico-muted-color, #646b79); margin: var(--pico-spacing, 0.75rem) 0 0.25rem; } .ds-mov-group { margin: 0; } .ds-mov-list { list-style: none; padding: 0; margin: 0; } .ds-mov-row { display: flex; align-items: center; gap: 0.6rem; padding: 0.5rem 0; border-bottom: 1px solid var(--pico-muted-border-color, #e7eaef); } .ds-mov-icon { flex: none; } .ds-mov-text { display: flex; flex-direction: column; min-width: 0; flex: 1; } .ds-mov-title { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; color: var(--pico-color, #373c44); } .ds-mov-meta { font-size: 0.8rem; color: var(--pico-muted-color, #646b79); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; } .ds-mov-amount { flex: none; white-space: nowrap; font-variant-numeric: tabular-nums; color: var(--pico-color, #373c44); }"
    },
    {
      "name": "Account Card",
      "kind": "card",
      "refersTo": "account-card",
      "description": "A tappable KPI card that opens the movements explaining its balance. Its 44x44 floor comes from .tappable in markup. The star marks the currency's default account and carries an aria-label.",
      "html": "<a class=\"ds-account-card\" href=\"#\"><article class=\"ds-kpi\"><p class=\"ds-kpi-label\">Mercado Pago <span class=\"ds-star\" aria-label=\"cuenta por defecto\">★</span></p><p class=\"ds-kpi-figure\">$312.480,55</p></article></a>",
      "css": ".ds-account-card { display: block; min-width: 44px; min-height: 44px; text-decoration: none; color: inherit; -webkit-tap-highlight-color: rgba(0,0,0,0.05); } .ds-account-card:active article { opacity: 0.7; } .ds-star { color: var(--pico-primary, #0172ad); }"
    },
    {
      "name": "Variacion Callout",
      "kind": "custom",
      "refersTo": "variacion-callout",
      "description": "Context, deliberately not a fourth KPI and deliberately not a card: it explains a balance that moved with no transaction behind it, styled as Telegram's own section footer. Never carries a status color.",
      "html": "<div class=\"ds-variacion\"><span>Variación de saldos</span><span class=\"ds-variacion-figure\">+$8.200,00</span><small>Ajustes y rendimientos, no plata gastada ni cobrada.</small></div>",
      "css": ".ds-variacion { display: grid; grid-template-columns: 1fr auto; gap: 0 0.5rem; align-items: baseline; padding: 0.5rem 0 0; color: var(--pico-muted-color, #646b79); font-size: 0.8rem; } .ds-variacion-figure { font-weight: 600; font-variant-numeric: tabular-nums; } .ds-variacion small { grid-column: 1 / -1; color: var(--pico-muted-color, #646b79); font-size: 0.75rem; }"
    },
    {
      "name": "Evolucion Heat Cell",
      "kind": "custom",
      "refersTo": null,
      "description": "The one dense surface. Two neutral shading steps against the row's own average, mixed from the ink so the tint follows the theme instead of the arbitrary accent. Its h1 title sits outside the horizontal scroller and the legend above the table.",
      "html": "<h1>Gasto por categoría</h1><div class=\"ds-evolution-scroll\"><table class=\"ds-evolution\"><tr><th scope=\"row\">Comida</th><td>$40.100</td><td class=\"ds-cell-mild\">$62.800</td><td class=\"ds-cell-high\">$98.400</td></tr></table></div>",
      "css": ".ds-evolution { border-collapse: separate; border-spacing: 0; width: auto; font-size: 0.85rem; color: var(--pico-color, #373c44); } .ds-evolution th, .ds-evolution td { padding: 0.35rem 0.4rem; border-bottom: 1px solid var(--pico-table-border-color, #e7eaef); white-space: nowrap; text-align: right; font-variant-numeric: tabular-nums; } .ds-evolution th[scope=\"row\"] { text-align: left; font-weight: 600; background: var(--pico-background-color, #fff); box-shadow: inset -1px 0 var(--pico-table-border-color, #e7eaef); } .ds-cell-mild { background-color: color-mix(in srgb, var(--pico-color, #373c44) 8%, transparent); } .ds-cell-high { background-color: color-mix(in srgb, var(--pico-color, #373c44) 18%, transparent); }"
    },
    {
      "name": "Category Table",
      "kind": "custom",
      "refersTo": null,
      "description": "Categorias' four-column table (name, total, %, por dia). The share came back on 2026-09-02 beside por dia, not instead of it. Four columns no longer fit a 360px phone, so the table sits in .cat-table-scroll (overflow-x: auto, overscroll-behavior-x: contain), the same pattern as Evolucion, with min-width: max-content and nowrap body cells so a long name or a large amount scrolls sideways instead of wrapping; unlike Evolucion there is no sticky column. Same 0.85rem/0.35rem-0.4rem compaction as Evolucion. The name column is a th scope=row carrying font-weight: inherit. % and por dia are muted money cells. A single p.muted below the table defines the por dia column, with its day count and dates computed from the same window the column divides by. It lives inside categoryTable, so the subcategory drill inherits it and an empty period renders no orphaned note.",
      "html": "<div class=\"ds-cat-table-scroll\"><table class=\"ds-cat-table\"><thead><tr><th scope=\"col\">Categoría</th><th scope=\"col\">Total</th><th scope=\"col\">%</th><th scope=\"col\">Por día</th></tr></thead><tbody><tr><th scope=\"row\"><a class=\"ds-row-link\" href=\"#\">🍝 Comida</a></th><td>$62.800</td><td class=\"ds-muted\">41%</td><td class=\"ds-muted\">$2.026</td></tr></tbody></table></div><p class=\"ds-cat-note\">Por día = total ÷ 31 días corridos, del 1 al 31 jul.</p>",
      "css": ".ds-cat-table-scroll { overflow-x: auto; overscroll-behavior-x: contain; } .ds-cat-table { font-size: 0.85rem; width: 100%; min-width: max-content; } .ds-cat-table thead th { padding: 0.35rem 0.4rem; } .ds-cat-table tbody th, .ds-cat-table tbody td { padding: 0 0.4rem; height: 44px; white-space: nowrap; } .ds-cat-table th[scope=\"row\"] { font-weight: inherit; text-align: left; } .ds-cat-table td { text-align: right; } .ds-muted { color: var(--tg-theme-hint-color, #646b79); } .ds-cat-note { font-size: 0.8rem; color: var(--tg-theme-hint-color, #646b79); margin: 0.5rem 0 0; }"
    },
    {
      "name": "Failure State",
      "kind": "custom",
      "refersTo": null,
      "description": "Every error carries its own way out. The two transient failures ship Reintentar, which re-fires the current route; the 401 deliberately does not, because retrying cannot mint a new initData.",
      "html": "<article><p>No se pudo cargar. Probá de nuevo en un momento.</p><button class=\"ds-btn-retry\" type=\"button\">Reintentar</button></article>",
      "css": ".ds-btn-retry { background: transparent; color: var(--pico-primary, #0172ad); border: 1px solid currentColor; border-radius: 0.25rem; padding: 0.4rem 0.9rem; min-height: 44px; font: inherit; } .ds-btn-retry:focus-visible { outline: none; box-shadow: 0 0 0 3px var(--pico-primary-focus, rgba(2,154,232,0.5)); }"
    },
    {
      "name": "Tappable Target",
      "kind": "custom",
      "refersTo": "tappable",
      "description": "The shared 44x44 tap-target rule for block-level elements: min-width/min-height 44px, inline-flex, align-items center — and, as of Phase 3, no justify-content center. Applied in markup to the account card and the three '← Volver' back links, all of which read left-aligned from the start of their own row or cell; an element that wants centering (the chip) states that itself. .tab-link and the period cursor arrows still carry their own hand-written copy of the 44px floor rather than this class.",
      "html": "<a class=\"ds-account-card ds-tappable\" href=\"#\">Mercado Pago</a>",
      "css": ".ds-tappable { min-width: 44px; min-height: 44px; display: inline-flex; align-items: center; }"
    },
    {
      "name": "Row Link",
      "kind": "custom",
      "refersTo": "row-link",
      "description": "A table-cell row link that fills its cell. The 44px pitch is set on the cell; the link stretches inside it.",
      "html": "<table><tbody><tr><td class=\"ds-row-cell\"><a class=\"ds-row-link\" href=\"#\">&#127837; Alimentacion</a></td></tr></tbody></table>",
      "css": ".ds-row-cell { height: 44px; padding: 0 0.4rem; border-bottom: 1px solid var(--pico-muted-border-color, #e7eaef); } .ds-row-link { display: flex; align-items: center; min-height: 44px; text-decoration: none; color: inherit; } .ds-row-link:active { opacity: 0.7; }"
    },
    {
      "name": "Page Total",
      "kind": "custom",
      "refersTo": "page-total",
      "description": "The figure that governs a whole screen, on one line with its label. Cuentas' Saldo hoy, Categorias' and its drill's Total.",
      "html": "<p class=\"ds-page-total\"><span>Total</span><span class=\"ds-money\">$412.300</span></p>",
      "css": ".ds-page-total { display: flex; justify-content: space-between; align-items: baseline; font-weight: 600; margin: 0 0 0.75rem; color: var(--pico-color, #373c44); font-family: system-ui, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; } .ds-page-total .ds-money { font-size: 1.5rem; line-height: 1.2; font-variant-numeric: tabular-nums; }"
    },
    {
      "name": "Evolucion Legend",
      "kind": "custom",
      "refersTo": "legend-swatch",
      "description": "The two-step heat legend, above the Evolucion table. Swatches are filled by the same classes the table cells use, so the legend cannot drift from what it explains.",
      "html": "<p class=\"ds-legend\"><span class=\"ds-swatch ds-swatch-mild\"></span> arriba del promedio de la fila &middot; <span class=\"ds-swatch ds-swatch-high\"></span> muy por encima &middot; en miles de $ &middot; Toca una categoria para abrir sus subcategorias.</p>",
      "css": ".ds-legend { font-size: 0.8rem; color: var(--pico-muted-color, #646b79); font-family: system-ui, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; margin: 0 0 0.75rem; } .ds-swatch { display: inline-block; width: 0.8rem; height: 0.8rem; border-radius: 0.25rem; vertical-align: -1px; } .ds-swatch-mild { background-color: color-mix(in srgb, var(--pico-primary, #0172ad) 22%, transparent); } .ds-swatch-high { background-color: color-mix(in srgb, var(--pico-primary, #0172ad) 45%, transparent); }"
    }
  ],
  "narrative": {
    "northStar": "The Account Statement",
    "overview": "The Mini App is a statement that closes. Every screen exists so the number at the top can be explained by the rows underneath it, and the user can walk from a total down to the individual movement that produced it without ever losing the window they were reading. Trust is the deliverable: a figure that does not reconcile is worse than no figure at all. That is why the account leaf prints an opening and a closing balance around a paginated list, and why footers come from aggregates over every row rather than from what has loaded on screen. The list itself loads 50 rows at a time behind an htmx infinite scroll, but the footer never depends on how many pages the user has scrolled through — it is computed the same way whether ten rows or a thousand have loaded.\n **The app does not own its palette. Telegram does.** Every color token is a `var(--tg-theme-*)` reference with a fallback, so the surface is painted in whatever theme the user chose in their client — including a custom one this system has never seen. That is the largest structural fact about this design system, and it inverts the usual relationship: brand does not live in hue here, because hue belongs to the user. It lives in density, in the tabular figure, and in the discipline that every number reconciles.\n The material is deliberately thin. Pico CSS 2.1.1 provides the base, `app.css` adds a set of scoped corrections, and every one of them exists because something specific broke on a 360px Telegram webview — a sticky column losing its border, a chip whose selected state measured 1.32:1, a tab bar sitting under the iOS home indicator, an inline `<a>` inside a table cell that a 44px rule could not reach. The system is a stack of small, argued repairs, not a theme. New work continues that discipline: reach for the Pico token before writing a value, and when you override, say why in the CSS.\n There is no admin surface in this app. An invitation-management screen lived here through 2026-08-25 and was removed outright at the user's request — route, templates, middleware and tab slot all gone, not merely hidden. Nothing below describes it; the OOB-into-a-reserved-slot technique it used is recorded once, in `internal/controller/miniapp/AGENTS.md`, as a pattern to reuse if a future view needs to depend on the viewer — not as a screen this file still owns.",
    "keyCharacteristics": [
      "Telegram-themed: 21 Pico color tokens are remapped onto `--tg-theme-*`, each with a fallback, so the app follows the user's client and still works outside it. **Every Pico colour the stylesheet touches is in that map** — the focus ring and the table hairline were the last two to escape it, and each escape looked exactly like a working default until someone with a custom theme opened the app.",
      "Flat and tonal: depth is a 1px hairline border and a card background, never a decorative shadow.",
      "Tabular numerals everywhere money appears, so columns of figures stay comparable.",
      "44px minimum on everything tappable — tabs, period arrows, account cards, back links, table row-links — reached by whichever of two mechanisms fits the element (a padded box, or an invisible overlay over a compact one).",
      "One heading level in the whole app (`#content h1`), used by every drill screen and deliberately absent from Resumen.",
      "No web fonts, no build step, no CDN: every dependency is vendored and pinned under `static/`.",
      "Color is reinforcement, never the only channel: the active tab carries a 2px bar, the default account carries a ★ with an `aria-label`."
    ],
    "rules": [
      {
        "name": "The app does not own its palette. Telegram does.** Every color token is a\n`var(--tg-theme-*)` reference with a fallback, so the surface is painted in\nwhatever theme the user chose in their client — including a custom one this\nsystem has never seen. That is the largest structural fact about this design\nsystem, and it inverts the usual relationship: brand does not live in hue here,\nbecause hue belongs to the user. It lives in density, in the tabular figure, and\nin the discipline that every number reconciles.\n\nThe material is deliberately thin. Pico CSS 2.1.1 provides the base, `app.css`\nadds a set of scoped corrections, and every one of them exists because\nsomething specific broke on a 360px Telegram webview — a sticky column losing\nits border, a chip whose selected state measured 1.32:1, a tab bar sitting\nunder the iOS home indicator, an inline `<a>` inside a table cell that a\n44px rule could not reach. The system is a stack of small, argued repairs, not\na theme. New work continues that discipline: reach for the Pico token before\nwriting a value, and when you override, say why in the CSS.\n\nThere is no admin surface in this app. An invitation-management screen lived\nhere through 2026-08-25 and was removed outright at the user's request — route,\ntemplates, middleware and tab slot all gone, not merely hidden. Nothing below\ndescribes it; the OOB-into-a-reserved-slot technique it used is recorded once,\nin `internal/controller/miniapp/AGENTS.md`, as a pattern to reuse if a future\nview needs to depend on the viewer — not as a screen this file still owns.\n\n**Key Characteristics:**\n\n- Telegram-themed: 21 Pico color tokens are remapped onto `--tg-theme-*`, each\n  with a fallback, so the app follows the user's client and still works outside\n  it. **Every Pico colour the stylesheet touches is in that map** — the focus\n  ring and the table hairline were the last two to escape it, and each escape\n  looked exactly like a working default until someone with a custom theme\n  opened the app.\n- Flat and tonal: depth is a 1px hairline border and a card background, never a\n  decorative shadow.\n- Tabular numerals everywhere money appears, so columns of figures stay\n  comparable.\n- 44px minimum on everything tappable — tabs, period arrows, account cards,\n  back links, table row-links — reached by whichever of two mechanisms fits\n  the element (a padded box, or an invisible overlay over a compact one).\n- One heading level in the whole app (`#content h1`), used by every drill\n  screen and deliberately absent from Resumen.\n- No web fonts, no build step, no CDN: every dependency is vendored and pinned\n  under `static/`.\n- Color is reinforcement, never the only channel: the active tab carries a 2px\n  bar, the default account carries a ★ with an `aria-label`.\n\n## Colors\n\nThere is no fixed palette. There is a **mapping**, plus three narrow exceptions\nwhere a fixed value is the right answer and the reason is written down.\n\nEvery token below resolves through `var(--tg-theme-*, <fallback>)`. Inside\nTelegram the variable wins; outside it — or on a client older than Bot API 7.0,\nwhich is when `section-bg-color`, `accent-text-color` and\n`section-separator-color` arrived — the fallback wins and the app looks exactly\nas it did before the mapping existed. The fallbacks are Pico's own light and\ndark palettes, which is why they differ per theme.\n\n### Primary\n\n- **Theme Accent** (`--tg-theme-accent-text-color` → `--tg-theme-link-color` →\n  Pico's azure): every interactive affordance — links, the active tab's label\n  and its 2px bar, the default account star — plus the expense series in every\n  chart and the Evolución heat shading.\n- **Theme Button** / **Theme Button Text** (`--tg-theme-button-color` /\n  `--tg-theme-button-text-color`): the selected chip's fill and its label.\n  Selection is a fill, not a hue shift; Pico's one-step\n  `primary → primary-hover` shift measures 1.32:1 in dark mode, below the 3:1\n  WCAG asks of a state.\n\n### Secondary\n\n- **Ledger Green** (`#1baf7a`): the income series in charts, and the one chart\n  color still fixed. Fixed because Telegram exposes a destructive color and\n  **no positive one** — there is nothing in the theme to derive it from.\n\n### Tertiary\n\n- **Account Slots** (eight fixed hexes, `slot-1` through `slot-8`): assigned by\n  position so an account's snapshot card and its trend line always carry the\n  same color. Color follows the entity, never the ordering of a particular\n  render. Fixed on purpose — see The Categorical Exception Rule.\n\n### Neutral\n\n- **Statement Ink** (`--tg-theme-text-color`): body text and every figure not\n  carrying status.\n- **Margin Grey** (`--tg-theme-hint-color`): labels above KPIs, the\n  date-and-category line under a movement title, disabled period arrows,\n  subcategory rows in Evolución. Most of the interface lives here.\n- **Hairline** (`--tg-theme-section-separator-color`, falling back to the hint\n  color): the 1px rule between movement rows, the tab bar's top border, every\n  table cell border, an unselected chip's edge.\n- **Paper** (`--tg-theme-secondary-bg-color`): the page and the tab bar, which\n  must be opaque because content scrolls beneath it.\n- **Card Paper** (`--tg-theme-section-bg-color`): cards and form fields — no\n  longer the Variación callout, which is borderless and fill-less (see the\n  Variación Callout component). The page/section split is Telegram's own: the\n  page sits on the secondary background and cards rest on top of it.\n\n### Status\n\n- **Positive Green** and **Negative Red**: a positive or negative Neto, and\n  nothing else. Both are **derived, not fixed** — see The Mixed Status Rule.\n\n### Named Rules\n\n**The Theme-Follows-The-User Rule",
        "body": "A color literal in `app.css` is a bug unless one of the three documented exceptions covers it (account slots, the income series, the tap-highlight). Reach for a Pico custom property; it is already mapped to Telegram. **Every `var(--tg-…)` carries a fallback** — a missing one does not fail loudly, it paints the element transparent or black, and only outside Telegram, where nobody is looking. `TestAppCSS_EveryTelegramVarHasFallback` enforces this.",
        "section": "overview"
      },
      {
        "name": "The Three Contexts Rule",
        "body": "Pico defines its palette in three theme contexts: `:root:not([data-theme=dark])`, `@media (prefers-color-scheme: dark)` with `:root:not([data-theme])`, and `[data-theme=dark]`. The remap must appear in **all three**, each with that theme's own fallbacks. One plain `:root` block loses on specificity; one high-specificity block wins everywhere and then its single-theme fallbacks override Pico's dark palette outside Telegram, silently turning dark mode light. `TestAppCSS_RemapsAllThreePicoThemeContexts` enforces this.",
        "section": "colors"
      },
      {
        "name": "The Mixed Status Rule",
        "body": "The Neto pair is derived, never fixed: `color-mix(in srgb, <hue> 70%, var(--pico-color))`. CSS cannot compute a contrast ratio, but it can mix a hue toward the theme's own text color, which Telegram has already guaranteed legible against its background — the hue survives and the lightness follows the theme. Measured, the fixed values gave 3.35:1 (green, light) and 3.75:1 (red, dark), passing AA only by counting as large text at 28px; mixed at 70% the four cases measure 4.82, 6.34, 6.50 and 4.83, clearing the 4.5:1 bar for *normal* text. Lowering the 70% buys contrast and costs saturation. The pair is asymmetric on purpose: red prefers `--tg-theme-destructive-text-color`, green has no counterpart to prefer.",
        "section": "colors"
      },
      {
        "name": "The One Status Rule",
        "body": "Positive Green and Negative Red appear on exactly one element per screen: the Neto figure. Variación de saldos is deliberately uncolored — a positive variation is not \"good\" the way a positive Neto is; it can be a CEDEAR that rose or a load that was missing, and tinting it green would assert something the system does not know.",
        "section": "colors"
      },
      {
        "name": "The Categorical Exception Rule",
        "body": "Account slot colors stay fixed hexes while everything else follows the theme, and that is not an unfinished migration. There, color *is* identity — the card and the trend line must match — and deriving eight mutually distinguishable hues from an arbitrary user theme is not a solvable problem. Same logic for Ledger Green: no positive color exists in the theme to derive it from.",
        "section": "colors"
      },
      {
        "name": "The Reinforcement Rule",
        "body": "No meaning is carried by color alone. The active tab gets a 2px geometric bar (muted grey against the accent is only 1.47:1); the default account gets a ★ glyph with an `aria-label`; the Evolución heat cells are named in a legend that sits above the table.",
        "section": "colors"
      },
      {
        "name": "The Tint-From-The-Ink Rule",
        "body": "A background tint mixes from the **text** colour (`--pico-color`), never from the accent. The accent is whatever the user's client hands us: mix a tint from it and the resulting cell can land anywhere, including right on top of the text's own luminance, with nothing to warn you. Mixing from the ink makes the tint a fixed fraction of the ink-to-paper distance, so it always moves the right way — darker on a light theme, lighter on a dark one — and the contrast that survives is bounded by construction rather than by luck. This is what the Evolución heat cells do (8% and 18%). Note the asymmetry with the status colours, which mix *toward* the ink to make a **foreground**: same token, opposite job, and swapping the two makes contrast worse, not better.",
        "section": "colors"
      },
      {
        "name": "The Browser-Surfaces Rule",
        "body": "The parts of the page nobody drew still belong to the design. Text selection and the caret are themed from the palette (`{colors.selection}`, accent caret); the focus ring is `{colors.focus-ring}`. A default-blue selection halo on a green Telegram theme is the cheapest possible tell that the page was assembled rather than built.",
        "section": "colors"
      },
      {
        "name": "The Number Wins Rule",
        "body": "The largest thing on any screen is always a number, never a label. The ramp encodes it: KPI figure (1.75rem) → page total (1.5rem) → heading (1.25rem) → body (1rem). A title that ties the figure it introduces is a title that competes with it, and on a phone the figure loses. Any new step slots below the money, not beside it.",
        "section": "typography"
      },
      {
        "name": "The One Heading Rule",
        "body": "There is exactly one heading level in the app: `#content h1`, styled at 1.25rem/1.15. Every drill screen (account leaf, subcategory drill, subcategory leaf, Evolución) opens with it. **Resumen shows no heading, and that is kept, argued behavior, not a gap** — the tab already says where you are, and the period header earns that space by saying *when* you are instead. It does carry `<h1 class=\"sr-only\">Resumen</h1>`: the decision is that the title should not take vertical space, not that the entry screen should be unreachable by heading navigation. Do not add an `<h2>` or a second heading weight anywhere; a screen that needs a visible title uses `<h1>`, a screen that doesn't hides one rather than going without.",
        "section": "typography"
      },
      {
        "name": "The Tabular Rule",
        "body": "Anything that is money wears `.money`. No exceptions, including inside table cells and inline balance lines.",
        "section": "typography"
      },
      {
        "name": "The Truncation Rule",
        "body": "A movement's title and its meta line each truncate to one line with an ellipsis. A long merchant name may never push the amount off the right edge — the amount is the reason the row exists.",
        "section": "typography"
      },
      {
        "name": "The 44px Rule",
        "body": "Anything tappable is at least 44×44 — but not every element gets there the same way, and which mechanism applies depends on what the element is:",
        "section": "layout"
      },
      {
        "name": "The Rows-Not-Tables Rule",
        "body": "A list of movements is a stack of `<section class=\"mov-group\">` day groups — each headed by a `GroupTitle` (`templates/group.templ`) and containing a `<ul class=\"mov-list\">` of flex rows — not a `<table>`. Four fields per movement in a narrow webview forces horizontal scrolling as a table; as rows, the text column flexes and truncates while the amount stays pinned right. The date lives once, on the group's `GroupTitle`, and not on each row — `GroupRowsByDay` (`templates/movement_row.go`) splits the list into consecutive same-date runs before render, so the row's meta line is free to hold only what actually varies between rows. Evolución and Categorías are the two real tables in the app, and both pay for it with a horizontal scroller; only Evolución also pins a sticky first column — Categorías' name column scrolls away with its four columns rather than staying pinned.",
        "section": "layout"
      },
      {
        "name": "The Carried-State Rule",
        "body": "Every view emits `AppState` — a hidden `#app-state` block of inputs the tab bar reads with `hx-include`. A view that renders without it silently resets the user's period on the way out, because `periodFromQuery` never 400s: it falls to defaults, and the filter appears to change by itself. `PeriodHeader` is today the rule's only consumer and it always shows the controls it carries state for — the historical case of a view carrying state without showing it (the removed Admin screen, which had nothing to filter) is gone, but the rule is written for the next one: a future controls-less view must still emit `AppState`, not skip it because there is nothing to show.",
        "section": "layout"
      },
      {
        "name": "The Flat-By-Default Rule",
        "body": "Surfaces are flat at rest, and a shadow must earn its place by solving a real layering problem the way the sticky column's inset does. Whether a genuinely floating layer (a sheet, a modal) may use an ambient shadow is **undecided** — the app has no such layer yet. Decide it when one arrives; do not smuggle one in as polish.",
        "section": "elevation & depth"
      },
      {
        "name": "The Way Out Is In The Error Rule",
        "body": "An error carries its own recovery. The two transient failures ship a **Reintentar** button that re-fires the current route (`htmx.ajax` against `location.pathname + location.search`); the 401 deliberately does **not**, because retrying cannot mint a new `initData` and a button that cannot work is worse than no button — its copy is the way out instead. Before this, a failed load was a dead end whose only exit was closing the app and reopening it from the chat.",
        "section": "components"
      }
    ],
    "dos": [
      "**Do** reach for a Pico custom property (`--pico-primary`, `--pico-muted-color`, `--pico-muted-border-color`, `--pico-background-color`, `--pico-card-background-color`) before writing a literal color. They are already mapped to the user's Telegram theme, so both themes and every custom one work for free.",
      "**Do** give every `var(--tg-…)` a fallback, always. Outside Telegram there is no failure — just a transparent or black element nobody sees.",
      "**Do** mirror any new theme mapping across all three of Pico's theme contexts.",
      "**Do** put `.money` on every monetary figure, wherever it renders.",
      "**Do** give anything tappable a 44×44 minimum — via `.tappable` for a block-level element, `.row-link` for an inline `<a>` inside a table cell, or an `::after` overlay for a compact control like the chip — and a deliberate pressed state.",
      "**Do** use `#content h1` for a drill screen's title, and nothing else as a heading. Resumen's stays `.sr-only` — see The One Heading Rule.",
      "**Do** finish the non-visual half of every screen you add: a name for any table or `role=\"group\"`, a text alternative for any canvas that is the only copy of its data, `aria-hidden` on anything purely decorative, and a `<th scope=\"row\">` on the column that identifies the row. This app's visual accessibility was good long before its semantics were, which is exactly how the gap stayed invisible.",
      "**Do** announce anything that replaces `#content` through the route status region. htmx swaps do it via `htmx:afterSwap`; anything writing `innerHTML` directly has to call `announce()` itself.",
      "**Do** carry every meaning in a second channel besides color — a glyph, a label, or geometry like the active tab's 2px bar.",
      "**Do** emit `@AppState(p)` from every view, even one with no period controls.",
      "**Do** record the reason for a Pico override **here**, not in the CSS. This package's code carries no comments — the code is the source of truth for behaviour, and this file is the source of truth for why the visual system is what it is. An override whose reason is written nowhere gets \"cleaned up\" by the next person, so an override and a line in this document ship together.",
      "**Do** keep the largest thing on any screen a number. See The Number Wins Rule.",
      "**Do** set a data row's height on the cell and let the link stretch inside it. Charging the touch floor on the link *and* padding on the cell pays for it twice.",
      "**Do** derive a footer total from an aggregate over all rows, never from the rows loaded on screen — each page loads 50 rows via infinite scroll, and the footer must hold at any scroll depth."
    ],
    "donts": [
      "**Don't** write a fixed color for anything but the three documented exceptions: account slots, the income series, and the tap-highlight rgba.",
      "**Don't** send a color from Go to a chart. Send a role and resolve it in `app.js`; that is what keeps chart data following the theme.",
      "**Don't** derive account slot colors from the theme. Color is identity there, and eight distinguishable hues cannot be pulled out of an arbitrary theme.",
      "**Don't** add a web font. `system-ui` is the stack; a webview on a bad connection cannot wait for a download to render text that is mostly numbers.",
      "**Don't** load anything from a CDN or add a build step. Vendor and pin it under `static/`, the way htmx 2.0.4, Pico 2.1.1 and Chart.js are.",
      "**Don't** add a decorative `box-shadow`, gradient, or backdrop blur. Depth is a 1px border and a background.",
      "**Don't** color the Variación figure — or any figure other than Neto — with a status hue.",
      "**Don't** turn a movement list into a `<table>`, or take the horizontal scroller off Evolución.",
      "**Don't** use the `hidden` attribute to hide a `.mov-row`; `display: flex` beats it.",
      "**Don't** give `.tappable` a `justify-content: center`; its current consumers all read left-aligned from the start of their row or cell. An element that wants centering states that itself, the way the chip does.",
      "**Don't** use `min-height` to make an inline `<a>` inside a table cell tappable — it does nothing there. Use `.row-link`.",
      "**Don't** go below 0.75rem for text, or above 1.75rem for a figure inside a card. A heading tops out at 1.25rem — below both money steps, on purpose.",
      "**Don't** tint a background from the accent. Mix it from `--pico-color` — see The Tint-From-The-Ink Rule.",
      "**Don't** leave a Pico colour out of the theme map. The test in `theme_test.go` is the guard; adding a token to the stylesheet without adding it there is how the focus ring and the table hairline both got missed.",
      "**Don't** put a script in the shell that only one view needs. Load it from a `data-` attribute the first time a view actually contains the element it draws, the way Chart.js is loaded.",
      "**Don't** reference a static asset without the `?v=` build hash. An unversioned URL is served `no-cache` on purpose, and the versioned one is `immutable` — a long cache without the hash is how a webview keeps a stale stylesheet forever.",
      "**Don't** write `overscroll-behavior` without an axis. Unqualified it sets both, and paired with an explicit `overflow-x: auto` it turns a horizontal scroller into a vertical scroll container that swallows the page's scroll.",
      "**Don't** add a comment to this package's code. Behaviour belongs in the code and in a test's name; the reasoning belongs in this file.",
      "**Don't** print a page total as a label above a figure. It is one line — `.balance-line page-total`, label left, figure right, `.money` on the figure.",
      "**Don't** let a legend sit below what it explains, or name fewer steps than the thing actually has."
    ]
  }
}

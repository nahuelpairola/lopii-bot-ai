services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: lopiibot
      POSTGRES_USER: lopiibot
      POSTGRES_PASSWORD: lopiibot
    ports:
      - "5432:5432"
    volumes:
      - ./db-data:/var/lib/postgresql/data